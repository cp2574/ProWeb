@{
    ViewBag.Title = "同学";
}
<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-sm-4">
        <h2>@ViewBag.Title</h2>
        <ol class="breadcrumb">
            <li>
                <a href="@Url.Action("Index", "Home")">主页</a>
            </li>
            <li class="active">
                <a href="@Url.Action("Classmate", "BosomFriend")"><strong>@ViewBag.Title</strong></a>
            </li>

        </ol>
    </div>
</div>
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row ">
        <div class="tabs-container">
            <ul class="nav nav-tabs">
                <li class="active"><a data-toggle="tab" href="#tab-1"> 小学同学</a></li>
                <li class=""><a data-toggle="tab" href="#tab-2">初中同学</a></li>
                <li class=""><a data-toggle="tab" href="#tab-3">高中同学</a></li>
                <li class=""><a data-toggle="tab" href="#tab-4">大学同学</a></li>

            </ul>
            <div class="tab-content ">
                <div id="tab-1" class="tab-pane active">
                    <div class="panel-body">
                        <p>
                        </p>
                    </div>
                </div>
                <div id="tab-2" class="tab-pane">
                    <div class="panel-body">
                        <p>
                        </p>
                    </div>
                </div>
                <div id="tab-3" class="tab-pane">
                    <div class="panel-body">
                        <p>
                        </p>
                    </div>
                </div>
                <div id="tab-4" class="tab-pane">
                    <div class="panel-body">
                        <p>
                        </p>
                    </div>
                </div>
            </div>


        </div>
    </div>
</div>

@section scripts{
    <script>
                $(function () {
                    createiframe("tab-1", 1)
                    $('.tabs-container .nav-tabs li a').click(function () {
                        var m = $(this).attr("href");
                        id = m.substring(1, m.length)
                        num = m.substring(m.lastIndexOf("-")+1, m.length);
                        createiframe(id,num)
                      

                    })
        })

        ///动态创建iframe
        function createiframe(id, num) {
            console.log(id); console.log(num);
            var iframe = document.createElement("iframe");
            iframe.style = "min-height:600px;width:100%;height:100%;";
            iframe.scrolling = "yes";
            iframe.frameBorder = "0";
            iframe.src = "../Classmate/Primary/" + num;
            $("#" + id + " p").text("");
            $("#" + id+" p").append(iframe);
        }
    </script>

}
